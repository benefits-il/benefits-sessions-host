# HOW-TO — איך מתחזקים את קונסולת-ההוראה

מדריך מעשי לבן. הקונסולה היא אתר HTML סטטי קטן: שלושה סוגי עמודים plus תיקיית דקים. עבוד תמיד בעותק המקומי, בדוק מקומית, ורק באישור — דוחפים.

---

## איך זה בנוי (חצי דקה)

- **שער** (`index.html`) ← **אינדקס** (`sessions.html`) ← **עמוד-קונסולה** (`session-<N>.html`).
- כל עמוד-קונסולה = שלוש רצועות (הכנה · בכיתה · בבית) בדיוק כמו אתר-התלמיד, וכל רצועה היא רצף **צעדים** ממוספרים. כל צעד = יחידה, עם שלושה דברים: סלוט-מצגת, לינקים, הערת-מעבר.
- **דקים** יושבים תחת `decks/session-<N>/<unit>/slides.html`.
- העיצוב: `delivery.css` (לא נוגעים) plus `console.css` (הרכיבים המנחה-בלבד).
- **קיפול רצועות**: כל כותרת-רצועה לחיצה מקפלת ופותחת את הגוף — חץ ירוק בקצה הכותרת (למטה=פתוח, שמאלה=מכווץ). מנוהל ב-`console.js`. ברירת-מחדל: רק "בכיתה" פתוחה, בלי שמירה בין רענונים. כל עמוד-מפגש טוען `<script src="console.js" defer></script>` לפני `</body>`.

---

## תרחיש 1 — לחבר דק אמיתי לצעד (הכי נפוץ)

כשדק של יחידה נבנה כ-HTML:

1. העתק את **כל תיקיית-הדק** מ-`benefits/course-content/session-<N>/<unit>/` ל-`decks/session-<N>/<unit>/` — כלומר `slides.html` **plus** תיקיית ה-`assets/` שלצידו. גרסת `slides.html` (עברית), לא `slides.en.html`.
2. בעמוד-הקונסולה, באותו צעד, החלף את כפתור `.deck-missing` ("דק בהכנה") בכפתור פעיל:

```
<a class="btn-stamp" href="decks/session-2/chains/slides.html" target="_blank" rel="noopener">פתח מצגת <i class="ti ti-presentation"></i></a>
```

3. בדוק מקומית שהדק נטען עם התמונות.

> העתקת ה-`slides.html` לבד **שוברת תמונות** — הדק מפנה ל-`assets/...jpg` יחסית. תמיד מעתיקים את התיקייה כולה.

---

## תרחיש 2 — לעדכן לינק של יחידה

כל צעד מחזיק רשימת `.linkchip` תחת "לפתיחה ושליחה". לינק קיים = `<a class="linkchip" href="..." target="_blank">`. לינק שעדיין אין לו כתובת = `<span class="linkchip is-tbd">`. כשמתקבלת הכתובת — הפוך אותו ל-`<a>` והסר `is-tbd`.

---

## תרחיש 3 — להוסיף צעד לרצועה

1. בעמוד-הקונסולה, באותה רצועה (`.lane-body`), הוסף בלוק `.step` חדש בסדר-ההוראה.
2. מלא: `.step__gidx` (מספר גלובלי רץ), `.num badge green` (תג מפגש.יחידה, למשל 2.6), שם, `.chip-studio`, סלוט-מצגת, לינקים, הערת-מעבר.
3. עדכן את מונה-הצעדים ב-`.ln-count` של כותרת-הרצועה אם קיים.

---

## תרחיש 4 — להוסיף מפגש

1. צור `session-<N>.html` בהעתקת `session-2.html`. עדכן כותרת, breadcrumb, ושלוש הרצועות לפי החלוקה של אותו מפגש באתר-התלמיד. (שורת ה-`console.js` בתחתית עוברת בהעתקה — ודא שהיא נשארת, כך הקיפול עובד מעצמו.)
2. ב-`sessions.html` החלף את כרטיס ה-stub של אותו מפגש בקישור פעיל (הסר `is-locked`, הפוך ל-`<a href="session-<N>.html">`).

---

## לבדוק מקומית (חובה)

מתוך התיקייה הזו:

```
python -m http.server 8000
```

פתח http://localhost:8000, עבור שער ← אינדקס ← מפגש, ולחץ כל כפתור-מצגת וכל לינק. (אם אין python: `npx serve`.)

---

## כללי זהב (אל תשבור)

- **מקור-אמת לחלוקה ולסדר** = אתר-התלמיד `benefits-sessions` plus ה-runsheets ב-`benefits/course-content/`. כשמשתנה שם — מעדכנים כאן.
- **דק = תיקייה שלמה** (`slides.html` plus `assets/`), לעולם לא ה-HTML לבד.
- **`delivery.css` לא נוגעים** — סגנון מנחה-בלבד נכנס ל-`console.css`.
- לא תוכן-תלמיד פנימה (רק לינקים) · לא הערות-דובר בנפרד (הן בדק) · לא run-sheet תפעולי (רק הערות-מעבר).
- עברית, חיצים שמאלה ←, בלי אימוג'ים בכותרות, בלי ערבוב עברית-אנגלית.
- בדוק מקומית לפני כל push. לא דוחפים ולא מפרסמים בלי אישור בן.

---

## איפה מה

| מה | איפה |
|---|---|
| קליפת הניווט | `index.html` · `sessions.html` · `session-<N>.html` |
| מצגות | `decks/session-<N>/<unit>/slides.html` plus `assets/` |
| עיצוב ירושה | `delivery.css` (לא נוגעים) |
| רכיבי-קונסולה | `console.css` |
| קיפול רצועות | `console.js` |
| סוכן תחזוקה | `CLAUDE.md` |
