# benefits-sessions-host — סוכן תחזוקה

זה הריפו של **קונסולת-ההוראה** של קורס Benefits — אתר HTML סטטי מנחה-בלבד, תאום של אתר-התלמיד `benefits-sessions`. לא העתק של תוכן-התלמיד אלא קוקפיט: מצגות plus לינקים plus הערות-מעבר, פר-מפגש ובסדר-ההוראה. כשבן אומר "תוסיף צעד / תחבר דק / תעדכן לינק" — אתה עורך את הקבצים, מראה לבן, ורק באישורו דוחף.

## איך האתר עובד (שלוש שכבות)

- **קליפה סטטית** — `index.html` (שער), `sessions.html` (אינדקס 5 מפגשים), `session-<N>.html` (עמוד-קונסולה). HTML ביד.
- **מצגות** — `decks/session-<N>/<unit>/slides.html` plus `assets/`. הדקים מועתקים מ-`benefits/course-content/session-<N>/<unit>/` (גרסת `slides.html` העברית, עם תיקיית ה-`assets/` שלה). הערות-הדובר בתוך הדק; נפתחות עם כפתור-ההצגה של הדק.
- **עיצוב** — `delivery.css` (ירושה זהה-בייט מאתר-התלמיד, **לא נוגעים**) plus `console.css` (רכיבים מנחה-בלבד).
- **קיפול רצועות** — `console.js` (vanilla, בלי תלות): כותרת-רצועה לחיצה מקפלת ופותחת את הגוף, חץ ירוק בקצה (למטה=פתוח, שמאלה=מכווץ ב-RTL). ברירת-מחדל בראש הקובץ: רק "בכיתה" פתוחה, בלי שמירה בין רענונים. כל `session-<N>.html` טוען `<script src="console.js" defer></script>` לפני `</body>`; סגנון הקיפול יושב ב-`console.css` בלבד.

## מבנה עמוד-קונסולה

שלוש רצועות `.lane` בדיוק כמו אתר-התלמיד: הכנה (`.lane--prep`) · בכיתה · בבית. כל רצועה = רצף `.step` ממוספרים. כל `.step` (יחידה אחת):

1. כותרת — `.step__gidx` (מספר גלובלי) plus `.num .badge` (תג מפגש.יחידה) plus שם plus `.chip-studio`.
2. `.deck-slot` — `.btn-stamp` "פתח מצגת" ל-`decks/...`, או `.deck-missing` "דק בהכנה".
3. `.linkset` — "לפתיחה ושליחה": `.linkchip` לעמוד-התלמיד plus כלים. לינק שעדיין אין לו כתובת = `.linkchip.is-tbd`.
4. `.xnote` — הערת-מעבר. אין מקור עדיין = `.xnote.is-tbd`.

## להוסיף צעד / לחבר דק

1. הוסף `.step` לרצועה הנכונה ב-`session-<N>.html`, בסדר-ההוראה, עם המספור הנכון.
2. כשהדק קיים — העתק את **כל תיקיית-הדק** (`slides.html` plus `assets/`) מ-`benefits/course-content/session-<N>/<unit>/` ל-`decks/session-<N>/<unit>/`, וכוון את `.btn-stamp` אליו. העתקת ה-HTML לבד שוברת תמונות.
3. אכלס לינקים ידועים; מה שחסר — `.is-tbd` plus לדווח לבן.

## להוסיף מפגש

1. צור `session-<N>.html` בהעתקת `session-2.html`, עדכן כותרת ורצועות לפי החלוקה באתר-התלמיד. (שורת ה-`console.js` בתחתית עוברת בהעתקה — שמור עליה, כך הקיפול עובד מעצמו.)
2. ב-`sessions.html` הפוך את כרטיס ה-stub לקישור פעיל.

## כללי בית

- **מקור-אמת לחלוקה ולסדר:** אתר-התלמיד `benefits-sessions` (הכנה/כיתה/בית) plus ה-runsheets ב-`benefits/course-content/`. כשמשתנה שם — מעדכנים כאן.
- עברית-first, חיצים שמאלה ←, בלי אימוג'ים, בלי ערבוב עברית-אנגלית באמצע משפט.
- `delivery.css` נשאר ירושה-נקייה — כל סגנון מנחה-בלבד נכנס ל-`console.css` בלבד.
- לא להעתיק תוכן-תלמיד פנימה (רק לינקים). לא presenter-notes בנפרד (הן בדק). לא run-sheet תפעולי (רק הערות-מעבר קלות).
- screenshot-verify מקומית לפני "מוכן". אל תשלח שום דבר החוצה ואל תדחוף ל-git בלי אישור בן.

## אירוח

ביניים, גישת-בן-בלבד רכה. נתיב/דומיין נסגרים בשער ה-publish (גייטד). דורש `.nojekyll` בשורש (קיים). כברירת-מחדל project-pages תחת `benefits-il.github.io/<repo>` עד שבן יכריע אחרת.
